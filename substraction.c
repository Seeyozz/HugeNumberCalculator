#include "main.h"

struct List* substraction(struct List* l1, struct List* l2)
{
    if (!l1 || !l2)
        return NULL;

    //int size1 = getSize(l1);
    //int size2 = getSize(l2);

    return NULL;
    
}